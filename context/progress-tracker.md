# Progress Tracker

## Current Status

**Phase:** Prototype-derived global foundation and reusable page sections implemented through the closing information/CTA sequence

**Last completed:** Created and pushed reusable HeadingAndText, InformationGrid, and CallToAction models; populated and published all four page uses; and verified the new closing sections against the desktop and mobile prototypes.

**Next:** Continue with the next approved prototype sections.

## Completed

- [x] Extracted the final colors, Inter type roles, responsive heading scale, 1200px bounds, wine CTA, sticky header, and blush footer from all four approved page prototypes.
- [x] Added `SiteHeader`, `SiteFooter`, and a shared arrow icon; the root layout queries the Settings singleton and the components render only CMS-owned content.
- [x] Expanded and pushed the Settings model with editable global header and footer fields through local Slice Machine.
- [x] Added focused server-render tests proving the shell renders CMS content and does not invent content when Settings fields are empty.
- [x] Populated the Settings document with the site name; About, Therapy & Support, and Get Started header links; Home, About, Therapy & Support, and Get Started footer links; and all prototype practice/legal footer copy.
- [x] Made footer navigation route-aware so the link for the current page is omitted while all labels and destinations remain CMS-managed.
- [x] Matched the production footer's bottom padding to the selected prototype at 40px on desktop and mobile, replacing the oversized 94px/72px values without changing other footer geometry.
- [x] Replaced the legacy Hero model with exactly Homepage Hero and Inner Page Hero, pushed the model through local Slice Machine, and kept page titles outside Rich Text as H1 elements.
- [x] Styled Hero lead, supporting, and helper Rich Text through shared global classes and the single `PrismicRichText` component.
- [x] Added the mobile-only hamburger and accessible right-side navigation drawer with focus trapping, Escape handling, body scroll lock, reduced-motion styling, and close-on-link behavior.
- [x] Populated and published prototype-derived Hero copy and media for Home, About, Therapy & Support, and Get Started; verified all four local routes render the CMS content and correct alternative text.
- [x] Corrected both Hero variations against the approved `prototypes/pages` source at the rendered desktop surface: the 1200px rail, 103.125px vertical padding, 68.75px column gap, title/lead/support measures, 481.25×687.5 media geometry, per-page crops, and Home CTA metrics now match at the 1375px verification viewport. Published the missing Home secondary CTA and Get Started emergency notice in Prismic instead of hard-coding either one.
- [x] Created and pushed the single-variation ImageAndText slice with generic content fields and independent background/top-border/bottom-border Booleans; implemented it with `Bounded`, `Heading`, `PrismicRichText`, and the shared `TextLink` arrow-link primitive.
- [x] Populated and published Home's “What it's like to work with me” wine section and About's top-divided “My background” section with exact prototype copy, paragraph structure, portraits, and alternative text.
- [x] Corrected and verified ImageAndText parity at 1375px and 390px: Home now uses the prototype's two-line 460px/40px heading, 491.3px paragraph measure, 20px paragraph rhythm, 24px paragraph-to-link gap, and 72px column gap; About matches its prototype grid, image, copy, heading, and paragraph geometry.
- [x] Created and pushed HeadingAndText with Side by Side and Two Columns variations; populated its Home, About, Therapy & Support, and Get Started uses with generic fields and shared Heading, Rich Text, and TextLink primitives.
- [x] Created and pushed InformationGrid and CallToAction as function-named, reusable slices with generic fields; implemented them through `Bounded`, `Heading`, `PrismicRichText`, and `ButtonLink` with no inline presentation.
- [x] Populated the Home, About, and Therapy & Support drafts with the shared “Meeting in person or online” grid and “Start with a conversation” CTA, and populated Get Started with its “Fees and session options” grid and inline location anchor.
- [x] Published Home, About, Therapy & Support, and Get Started together. Verified the live CMS content on all four local routes: the shared desktop InformationGrid is 593.914px tall, the Get Started grid is 644.914px, and CallToAction is 410.797px, with exact 1200px rails and 103.125px section padding at 1375px. Verified the 390px stack, 48px mobile padding, 350px rail/button, 26px/27.82px headings, CTA action offset, Get Started item rhythm, and `#location` inline link. Also matched the prototype's intermediate breakpoints: Home/Therapy retain two information columns at 1000px while About/Get Started stack, and the CTA remains 263.35px wide at 700px before becoming full-width below 600px.
- [x] Applied the ElevenLabs-informed shared typography, warm neutral colors, three button sizes, card tokens, and page bounds; removed visible eyebrows without changing models.
- [x] Generated and visually inspected the one-page client style PDF; checked desktop/mobile shared components, font loading, keyboard focus, and CTA anchor navigation.
- [x] Verified lint, TypeScript, all five focused slice-render tests, and production build. Build required network access for Inter; Prismic retried an expired public ref successfully.

- [x] Scaffolded Next.js 16 App Router with TypeScript and Tailwind CSS.
- [x] Installed Prismic client, React, Next.js, and Slice Machine integrations.
- [x] Connected Slice Machine to `mental-health-counseling`.
- [x] Authenticated the Prismic CLI account used during setup.
- [x] Synchronized the existing `home` custom type.
- [x] Added preview, exit-preview, revalidation, and slice simulator routes.
- [x] Verified linting, TypeScript, production build, the app, simulator route, and Slice Machine.
- [x] Removed unrelated Supabase, admin dashboard, storage, auth, and nonexistent UI-system guidance from project context.
- [x] Added `Bounded` with editor-friendly spacing and width options for slice layouts.
- [x] Added shared `Heading`, `PrismicLink`, and `ButtonLink` primitives using the semantic colors in `globals.css`.
- [x] Added one `PrismicRichText` renderer for typography, lists, code blocks, and inline links.
- [x] Established that Rich Text body content starts at H2 and excludes embedded media.
- [x] Added lazy, accessible YouTube and TikTok player components for dedicated video fields.
- [x] Added Image Full Width and Two Up variations.
- [x] Added Video YouTube, Uploaded Video, and External Video variations.
- [x] Added Hero Centered, Split, and Minimal variations with repeatable styled CTAs.
- [x] Added Content Text Only, Centered Text, Image Right, and Image Left variations with shared Rich Text, CTA, spacing, and width controls.
- [x] Pushed the Content slice model to the Page Builder and added automated render tests for its centered and image-left variations.
- [x] Added editor-facing Section spacing and Content width controls to every variation.
- [x] Added automated render tests for Hero, Image, and Video.
- [x] Rendered the non-repeatable Home document at `/` with Prismic metadata.
- [x] Rendered repeatable Page documents at `/:uid` with metadata and static parameters.

## Current Limitations

- The shared visual foundation is implemented; full page compositions and photography remain pending.
- Slice screenshots and representative repository content have not been added yet.
- Rose and wine are optional accents awaiting client feedback; buttons remain neutral.

## Upcoming

- Verify previews and revalidation against published and draft content.
- Create a test page containing every slice variation and perform responsive visual QA.
- Add Slice Machine screenshots once representative mock data is available.

## Decisions

### September 13, 2026: Homepage refinement variants

- Revised v4 from browser feedback: the Gladys introduction is now a square-cornered, full-bleed stone band, with its photo aligned to the hero’s content column; parent support is an open two-column editorial reading section rather than a wine panel; and major sections now have deliberately larger separation. Hero, audience rows, footer, assets, and wording remain unchanged. Verified rendered desktop and 390px mobile behavior with no horizontal overflow.
- Recalibrated v4 spacing after a second browser review: the hero-to-introduction gap is halved; sections use a single moderate vertical cadence; decorative parent, audience, credential, and section dividers are removed; and all non-hero/non-footer section links match v1’s black fine-underlined default with the wine hover. Footer and sessions-specific dividers are preserved. Verified desktop and 390px rendering, no audience-row borders, black default links, and no horizontal overflow.
- Corrected v4’s shared grid after visual review: the introduction and parent-support sections now use identical 550px / 72px / 578px column geometry, aligning their right-hand copy precisely. Restored 88px vertical padding inside the desktop introduction (48px/56px mobile), while spacing between sections is 104px desktop and 88px mobile. Verified the rendered column positions, spacing, and no-overflow mobile state.
- Rebuilt v4–v6 below the hero following rejection of the initial studies. Hero HTML and original hero CSS remain unchanged; all existing text nodes are preserved.
- Added approved generated editorial imagery for parents, teens and adults. V4 uses open image rows and a horizontal sessions composition; v5 uses a photographic audience spread and a unified split consultation panel; v6 uses a keyboard/pointer-controlled audience photograph and an open sessions layout.
- Replaced the small footer brand treatment with a full-width signature and regrouped navigation, credentials, location and emergency information. No application or Prismic changes.
- Verified the local in-app browser at 1440px and 390px, image loading, no mobile horizontal overflow, and v6 keyboard image selection. JavaScript syntax and focused layout scan pass. Consultation and secondary page links remain prototype-only.

- Added standalone homepage v4 (Considered), v5 (Open House), and v6 (In Focus) under `prototypes/homepage`, based on the approved research direction and preserved v1 wording/palette.
- Stronger headings, reduced body scale, v2-inspired portrait grouping, image-assisted audience rows, composed sessions areas and footer variants. V6 image selection responds to pointer entry and keyboard focus.
- Existing v1–v3 layouts preserved; their switchers now link to all six studies. Supplied Gladys portraits are provisional service-layout imagery.
- Inspected desktop and 390px mobile; all three fit without horizontal overflow. No Next.js application or Prismic changes. See the prototype README for scope and verification limits.

### September 9, 2026: Hero artwork prototypes

- Created six isolated static hero studies in `prototypes/hero/`, using the revised Word-document hero copy, current Inter/type/color foundation and matching Medium primary/secondary button styling.
- Explored tree-framed New York, supplied Gladys portrait in a brain silhouette, two brain collages, a translucent rose head, and a New York landscape within a brain. Five generated artworks and the supplied portrait are stored locally with the prototype.
- Verified all six desktop variants, comparison selection, and a 390px embedded mobile preview in the Codex in-app browser. JavaScript syntax check passed. No application build impact; production source and Prismic content were not changed by this prototype task.
- Local review only. No winning direction selected or published. Run instructions and source/assessment notes are in `prototypes/hero/README.md`.

- V4 homepage spacing follow-up: restored v1's 01–04 section labels and their original typography; moved the parent-support link below its explanatory copy. Standardized major section gaps to 144px desktop / 96px mobile, with separate heading and audience-row spacing. Restored introduction band padding around both columns and aligned the photo/copy top edges and parent-copy rail. Verified desktop and 390px mobile in the in-app browser with no horizontal overflow; existing copy, hero markup and footer markup preserved. Static prototype only; no application build impact.

- Applied approved v4 background redistribution: full-bleed stone audience band with constrained inner content; sessions background/radius and horizontal inset removed. Existing 144px desktop / 96px mobile transitions are split between band padding and outside spacing rather than compounded. Desktop and 390px mobile inspected in the in-app browser; no horizontal overflow. Hero/footer preserved; numbered labels and content unchanged. Layout detector returned no findings in limited regex fallback mode (parser dependencies unavailable).

- V4 composition follow-up: removed numbered labels (retaining one descriptive heading per section), returned audiences to paper background, retained unboxed sessions, and halved the hero-to-introduction gap to 68px desktop / 40px mobile. Footer now places address left and navigation right, with credentials beside the emergency notice below the name. Substantive content and imagery preserved. Static prototype only.

- This site is Next.js plus Prismic only.
- Supabase is not part of the architecture.
- Authentication, databases, storage, UI libraries, and other services will be added only when required.
- Content models remain owned by Slice Machine.
- Shared Rich Text styling is owned by `src/app/components/PrismicRichText.tsx`, not individual slices.
- H1 is not used in Rich Text body content; page titles are separate from editorial prose.
- Context files describe only systems that actually exist.

- September 13, 2026: Rebuilt the three About studies under `prototypes/pages` within homepage v4's existing palette and shared chrome. Preserved every supplied About paragraph and the header/footer markup; Home.html unchanged. Added a distinct paper consultation close before the stone footer and corrected the About demo switcher sizing. Desktop browser inspection and source-preservation checks only; no production app changes. Therapy/Support and Get Started remain explicit prototype placeholders.

- September 13, 2026: Recomposed the three About studies in `prototypes/pages` after the previous set read as flat. Root causes were a type ramp with only two registers (a 64–72px title directly above 15px body), sections built as a short heading column beside a deep body column so the shorter one always orphaned, and no separation device between chapters. Introduced a documented ramp with `lead` and `statement` registers and 17px body, moved every heading-beside-body pairing into a dedicated rail with a single reading measure, and separated chapters by hairline or surface change only. Portrait (shared baseline with a tall standing portrait), Editorial (full-width title, portrait bleeding off the right edge, one chapter inverted onto a dark field), and Column (18px continuous read against a sticky portrait rail with a column rule). Closing invitation now closes at opening scale; portraits unrounded; review switcher lifted off content. Added one on-load reveal, opted in before first paint and disabled under reduced motion. All supplied About copy preserved verbatim. Inspected at 1440, 900 and 390 px; captures in `.impeccable/review/about-v2/`. Design detector returns no findings. `DESIGN.md` and the prototype README record the ramp, tokens and open items. Static prototype only; Home.html, production source and Prismic unchanged.

- September 13, 2026 (revision): Reworked About v1 and v2 on review feedback. Removed all entrance animation, the scroll reveal, and every sticky element. Removed the decorative hairlines used as chapter separators, pull-quote rules and the credential underline; separation is now space and surface only, with a single full-bleed rule above the closing invitation. Moved every section heading to full width on top of its copy and retired the narrow heading rail, which was the source of both the cramped headings and the orphaned columns. Locked section rhythm to one value by giving every section half a chapter of padding per side, so paper-to-paper and paper-to-band boundaries measure the same. Removed the MHC-LP credential from the page body, since the footer and "My background" both already carry it. v2: opening copy now shares the title's left edge, and the portrait sits inside the container rather than bleeding off the right. v3 left as reference only, with animation stripped. Reviewed at 1440, 900 and 390 px; design detector returns no findings. Static prototype only.

- September 13, 2026 (spacing pass): Tripled the About section rhythm to clamp(96px, 15vw, 224px) and removed the breakpoint overrides that had been reducing it at narrower widths, so space rather than rules separates the sections at every size. Fixed two layout defects: the v1 opening was bottom-aligned to its portrait, which pushed the page title well down the column, and portraits set beside copy were overhanging the end of their text by up to 230px. The v1 opening now aligns to the top with the portrait taking the row height, and side portraits are capped near the height of the copy. Removed the portrait from the v2 opening, which becomes typographic; her placement in that opening is an open question after a right-edge bleed and an inset column were both rejected. Detector clean; reviewed at 1482, 900 and 390 px.
- September 13, 2026 (type pass): Collapsed four section-heading sizes into one. Chapter headings, portrait-paired headings and the closing heading were running 56, 44 and 60px, with a fourth at 62px on v2's dark band, all for the same role. Rebalanced the vertical metrics: heading-to-body went from 53px to 76px, paragraph spacing from 26px to 20px, and body leading from 1.76 to 1.62, so a heading now reads as belonging to its whole block rather than to its first paragraph. Detector clean.
- September 13, 2026 (surface pass): Removed the last border from the About pages and made every section boundary a change of ground instead, alternating paper and stone with the dark field on v2. The hero and the approach chapter were both on paper, which is why 224px of space still read as one continuous section. Centred the v1 opening copy against its portrait. Set the opening lead to 30ch and the paragraph under it to 44ch, chosen by probing the real line breaks so the paragraph fills its last line rather than stranding "for"; added text-wrap: pretty to body copy generally. Narrowed the page measure from 68ch to 62ch. Detector clean.
- September 13, 2026 (hero pass): Gave the About opening the same top padding as every other section; it had a reduced value that put it too close to the header. Enlarged the v1 portrait from 400x600 to 520x700. Moved the heading-to-content distance onto a single --heading-gap variable shared by h1 and h2, so the page title now sits 74px above its lead exactly like every section heading. Widened the opening paragraph to 52ch so it runs wider than the lead while still filling its last line. Verified measured gaps: h1 74px and all four h2 gaps 74px on both pages. Detector clean.
- September 13, 2026 (heading scale): Reduced the single section-heading size by 20 percent, from clamp(32px, 3.9vw, 56px) to clamp(26px, 3.1vw, 45px). Hero titles are unchanged at 72px on v1 and 96px on v2, so the gap between the page title and the section headings widened. Line counts for every heading on both pages are unchanged. Detector clean.
- September 13, 2026 (measure pass): Made every body paragraph the same length. They had been rendering at 624, 563 and 662px because the 62ch measure was wider than most columns, so the grid was setting the line length instead of the measure. Dropped the measure to 52ch (555px), below the narrowest column, and raised the stacking breakpoint for both the two-column reading sections and the portrait sections to 1280px so no column squeezes under it. Verified 555px at every width from 1482 down to 768. Removed max-width from the .reading wrappers: a ch there resolves against the inherited 16px, not the paragraph's 17px, which was clipping paragraphs to 523px. Rebuilt the portrait-plus-copy sections on the same halves the reading sections use, so that copy starts on the same line as the column above it, and let the portrait bleed past the section padding at both ends. Removed three figcaptions that were clipped out of sight by overflow:hidden but still exposed to assistive technology. Detector clean.
- September 13, 2026 (wine band): Moved the v1 background chapter from stone to wine so the page is not running two identical stone bands; the wine already carries the closing panel on the home page. Contrast measured at 8.19:1 for the heading and 6.93:1 for body copy. v2 keeps stone there, since its second band is the dark field and there is no repetition to break up.
- September 13, 2026 (wine placement): Moved the wine band off the background chapter and onto the approach chapter. Behind the portrait the plum fought the cool window light and green jacket in the photograph, and the inset image read as a rectangle pasted onto the band rather than part of it; the approach chapter is text only, so nothing competes. The background chapter is stone again, where its bleeding portrait sits comfortably. Recoloured the statement on wine, which is normally set in wine itself and would have vanished, using the same accent tint the dark field band already uses. Contrast: heading 8.19:1, body 6.93:1, statement 5.40:1. Detector clean.
- September 13, 2026 (close + title): Aligned the closing button and its helper with the footer link column by giving the closing grid a 320px right track, matching the footer's constant 320px link column; all three now start at the same x. Reduced the v1 page title by roughly 12 percent, from 72px to 63px. v2's 96px title is unchanged, since that opening carries no portrait and the title is the whole composition. Detector clean.
- September 13, 2026 (gutter cap): Capped the gutter at 88px instead of 96px. On a 1920 screen the wider gutter shrank the two split reading columns to 552px, three pixels under the measure, so those paragraphs came out shorter than every other one. Paragraph width now verified at 555px from 2560 down to 768, and the closing button, its helper and the footer link all share a left edge at every width where the closing section is two columns.
- September 13, 2026 (footer pass): The footer's first link pointed at the page it was already on. It now reads Home and goes to Home.html on all three About studies, while Home keeps About Gladys there; the reciprocal link is the one users actually need from each page. Separated the copyright line from the legal block above it: the fine print was a three-tier stack with 24px between every tier, including the 24px under the rule, so the copyright read as a third paragraph of the identity column rather than a closing line. It now sits 52px below, roughly double the tier spacing, using margin rather than a second rule. Shared footer, so the change applies to Home as well. Verified on Home, v1 and v2 at 1920 and 390 px, and by following the link.
- September 13, 2026 (Therapy v1 balance pass): Refined the secondary hero paragraph and matched its 32px lead-to-body gap to About v1. Centered the audience number, name and summary vertically within each toggle row; increased closed-row and expanded-panel spacing; and centered each fixed-height image against its reading column. Reduced Parents and Teens to four paragraphs each while preserving their meaning, the Circle of Security qualification and the parent consultation note; Adults remains three paragraphs. Reduced the audience labels from the 45px chapter-heading scale to a regular-weight 19–21px body-gray label so the open chapter heading holds the hierarchy. Extended the accordion's top, row and closing rules edge to edge while keeping its content on the existing wrap. Increased desktop row padding to 56px and open-panel padding to 56px/96px, with proportional mobile spacing. Matched the open-panel image column and each closed-row summary column to the hero's responsive 320–520px image column so the sentence and image share the same left edge and width. Brought the audience labels 40px closer to their numbers without changing the summary/image alignment. Static prototype only.
- September 13, 2026 (Therapy prototype selection): Retired the Therapy 2 and Therapy 3 entries from the prototype switcher and renamed the remaining Therapy v1 entry to Therapy across the page demos. Updated the prototype README and switcher note to reflect the single selected composition.
- September 14, 2026 (portrait correction): Kept the selected homepage portrait and replaced About v1's "My background" image with the distinct full-body seated portrait from the five-image set, preserving the existing image treatment and layout.
- September 14, 2026 (CTA color): Changed the shared solid button fill from black to the existing wine token (`#69434B`), keeping the paper text and existing hover treatment. Refreshed the homepage stylesheet cache and verified the hero CTA in the in-app browser.
- September 14, 2026 (About color test): Applied `#D38C92` to About v1's "Therapy should feel like a real conversation" band only, switching its heading/body copy to dark readable tones and leaving the other page surfaces unchanged. Verified the rendered band in the in-app browser.
- September 14, 2026 (About color test revision): Set the same band’s heading, paragraphs and statement to pure white for direct comparison against `#D38C92`; the test is live in the in-app browser.
- September 14, 2026 (About color test reverted): Removed the `#D38C92` and white-text experiment from the conversation band, restoring the original wine background and text palette.
- September 14, 2026 (About color test): Tested a muted terracotta derived from the couch in Gladys's portrait (`#A66254`) on the conversation band with light text. Verified the rendered result in the in-app browser.
- September 14, 2026 (About color test refinement): Lightened the couch-derived terracotta to `#B87566` while keeping the light text treatment. Verified the updated band in the in-app browser.
- September 14, 2026 (About color test): Tested the darker dusty mauve from the user-provided circled flower reference (`#7A4F59`) on the conversation band with light text.
- September 14, 2026 (Shared surface color): Replaced the shared gray stone background with a barely-there blush pink (`#FAF0F1`) across the canonical prototype surfaces, including the footer and “Meeting in person or online” section.
- September 14, 2026 (Therapy surface rhythm): Removed the blush background from the “Meeting in person or online” section so it follows the wine approaches band on paper instead of stacking two colored surfaces.
- September 14, 2026 (Therapy CTA grouping): Tightened the boundary between “Meeting in person or online” and “Start with a conversation” so the three CTA choices read as one closing decision path, while leaving their content and order unchanged.
- September 14, 2026 (Sticky header): Made the shared v4 header sticky with a translucent pale-pink glass treatment and backdrop blur so navigation stays available while scrolling across Home, About v1 and Therapy v1.
- September 14, 2026 (Sticky header refinement): Lightened the translucent header blush to `rgba(253,249,250,.72)` so the glass effect stays subtle rather than reading as a strong pink.
- September 14, 2026 (Sticky header refinement): Removed the header’s bottom border; the light glass surface now provides the separation on its own.
- September 14, 2026 (Shared blush refinement): Lightened the blush surface everywhere to `#FCF6F6` and matched the sticky header to the same softer pink so the site stays consistent without becoming washed out.
- September 14, 2026 (Blush color refinement): Shifted the shared surface from gray-leaning pink to a clearer light pink (`#F7E1E6`) and matched the sticky header tint to it.
- September 14, 2026 (Blush color refinement): Brightened the shared blush to a lighter, cleaner rose pink (`#FBE7EE`) to remove the gray/dark cast while keeping it soft.
- September 14, 2026 (White-pink refinement): Reduced the shared blush to a near-white pink (`#FFF4F7`) with a very light rose tint, including the sticky header.
- September 14, 2026 (Header refinement): Kept the header sticky but changed its surface to white and replaced the borderless edge with a subtle shadow for separation.
- September 14, 2026 (Header refinement): Replaced the rejected shadow with a subtle one-pixel border on the white sticky header.
- September 14, 2026 (CTA hover): Replaced the solid wine button’s dark-gray hover with a lighter wine hover so the interaction stays within the button color family.
- September 14, 2026 (Footer surface): Separated the shared footer from the blush sessions surface with a warm off-white/light-gray background (`#F5F3F1`) across the canonical v4 pages.
- September 14, 2026 (Surface correction): Reverted the footer to the shared blush and applied the warm off-white/light-gray only to the “Sessions and getting started” panel, matching the annotated target.
- September 14, 2026 (Session surface refinement): Replaced the gray session-panel test with a near-white blush (`#F8F6F7`) that is slightly darker than the page background for contrast without a gray cast.
- September 14, 2026 (Session surface refinement): Lightened the session panel again to an almost-white blush (`#FBF9FA`) so it reads as off-white with only a subtle contrast against the page background.
- September 14, 2026 (Session surface refinement): Returned the session facts side to paper white and added a single thin wine edge on its left side; the wine consultation side and pink footer remain unchanged.
- September 14, 2026 (Session surface refinement): Replaced the single edge with a complete thin wine outline around the white session-facts card, using matching rounded outer corners and responsive top corners on mobile.
- September 14, 2026 (Session surface refinement): Softened the session-facts card outline from wine to the shared pale-pink token while keeping the white fill and matching rounded corners.
- September 14, 2026 (Session surface refinement): Increased the session-facts outline to a visible pale rose (`#E8C8CF`) so the white card has clear separation without returning to a dark wine border.
- September 14, 2026 (Pages handoff cleanup): Made `prototypes/pages` self-contained for client delivery by copying the selected pages’ images, Inter font, shared CSS, and shared JavaScript into the folder, rewriting all runtime references to local files, and removing the retired page studies and placeholder. Verified Home, About v1, and Therapy v1 load their local images and styles from the Pages root.
- September 16, 2026 (Homepage hero desktop experiment): Removed the homepage hero's desktop-only bottom inset so the sculpture meets the following section. The copy block is vertically centered within the complete shortened hero with balanced space above and below, while the sculpture scales independently on wide desktops and remains bottom-aligned. After reviewing a 10-percent enlargement, reduced both artwork dimensions by 5 percent from that version. Inner-page heroes and tablet/mobile rules are unchanged, and the hero/artwork/next-section boundary remains aligned.
- September 16, 2026 (Full-image homepage hero and header): Added the `Full Image Homepage Hero` Slice Machine variation without replacing the existing Homepage or Inner Page variations. The full-image variation preserves the homepage copy and CTA geometry while placing the supplied sunlit-chair image behind an `80svh` visual hero. On this homepage variation only, the sticky header is transparent over the hero, hides while scrolling down, and returns while scrolling up; non-home headers retain their existing white treatment. Verified the transparent top state, taller hero, paper transition into the user-updated introduction section, and both scroll directions in the Codex in-app browser.
- September 16, 2026 (Hero copy contrast): Strengthened the full-image hero's existing warm left-to-right overlay and added a soft bottom-left radial scrim behind the CTA helper line. Text colors remain unchanged, and the overlay fades before the chair so the photograph stays open on the right. Corrected the image and overlay offsets to include the header's one-pixel border, removing the exposed page-background line at the top edge. Verified the rendered desktop hero and seamless zero-pixel image origin in the Codex in-app browser.
- September 16, 2026 (Site-wide returning header): Kept the homepage header fully transparent only at the absolute top. Across every page, the header hides throughout downward scrolling and returns only on upward scrolling as a full-width translucent warm glass surface with blur, subtle saturation and a light shadow. Replaced the rejected inset rounded-card treatment and lengthened the transform/opacity easing so the reveal is smooth rather than an abrupt white jump. Inner-page headers start as a borderless, shadowless paper surface that merges into the page instead of falsely implying content is already passing behind glass; glass is reserved for the returned overlay state where content actually does pass beneath it. Verified initial and scroll states on both Home and About in the Codex in-app browser.
- September 16, 2026 (Header direction hysteresis): Decoupled the scrolled state from the visible glass-return state. On the homepage, leaving the top while scrolling down now keeps the header transparent as it exits; glass is applied only after at least 18px of deliberate upward travel. A 4px downward threshold hides the header, while separate accumulated up/down travel prevents momentum jitter from falsely revealing it. Verified the first downward step, continuous downward scroll, and deliberate upward return over the live hero.
- September 16, 2026 (Who I work with production section): Added a reusable `WhoIWorkWith` Prismic slice to the Home page model and implemented it with the existing `Bounded`, `Heading`, `PrismicRichText`, `PrismicNextImage`, and `TextLink` primitives. The section preserves the selected prototype’s full-width paper band, paired heading/note rail, three spacious audience rows, restrained image treatment, and exact supplied copy while adapting the broken prototype mobile layout into a clean single-column reading order. Added the three audience images, accessible alt text, and therapy links to the Home document draft in Prismic; final publication and rendered desktop/mobile review are pending.
- September 16, 2026 (Who I work with image alignment): Corrected the three desktop audience images to the prototype’s label-aware horizontal position without changing the row, heading, description, image size, or mobile composition. At the 1761px comparison viewport, production and prototype images now share the exact `547.617px` x-coordinate while the description rail remains unchanged at `902.742px`; the mobile rule explicitly clears the desktop offset.
- September 16, 2026 (Heading and Text wine treatment): Added a model-controlled wine background to every HeadingAndText variation, with paper headings, on-wine body copy, rose-accent statement copy, light editorial links, and no bottom rule. Added the reusable Conversation Chapter variation for the About prototype’s full-width heading followed by a wider reading column and larger statement column. Verified the exact prototype copy in Slice Simulator at 1440px and 390px; focused HeadingAndText tests, lint, TypeScript, and the production build pass. Published the Conversation Chapter immediately after the About hero in Prismic and verified the wine band, exact copy, and following chapter order on the rendered `/about` page.
- September 16, 2026 (Homepage header first paint): Removed the white header flash on hard refresh by making the server-rendered full-image hero select the transparent header treatment in CSS before client hydration. The observer still owns scroll direction: downward scrolling hides the header and upward scrolling restores the translucent glass surface. Added a focused regression check and verified the transparent top state plus hidden-down/visible-up states on the rendered homepage; lint, TypeScript, build, and diff checks pass.
- September 16, 2026 (Route navigation scroll reset): Removed the global smooth-scroll rule that was animating Next.js cross-page scroll resets. Navigation from a scrolled page now opens the destination immediately at `scrollY: 0`; the sticky header's own hide/return transitions remain unchanged. Added a focused regression check and verified the About-to-Therapy navigation path in the in-app browser.
- September 16, 2026 (Therapy audience accordion): Created and pushed the reusable `AudienceAccordion` slice through Slice Machine, using the existing `Bounded`, `Heading`, `PrismicRichText`, `PrismicNextImage`, and `TextLink` primitives. Matched the Therapy prototype's wine band, full-width rules, row alignment, open-panel copy/image geometry, single-open behavior, closable active row, URL hashes, direct-hash opening, keyboard focus movement, mobile stack, and reduced-motion treatment. Added the exact Parents, Teens, and Adults copy, CTA links, images, and alternative text to the Therapy & Support document in Prismic. Focused rendering tests, TypeScript, lint, build, and final live-browser publication verification are recorded with the implementation handoff.
- September 16, 2026 (Therapy accordion heading width): Corrected the open-panel heading measure to the prototype's rendered 453.96px desktop width. Live and prototype now match at two lines for Parents, two for Teens, and three for Adults without changing the surrounding grid or copy.
- September 16, 2026 (Consultation link target): Standardized every consultation CTA on `/get-started#request`, including the homepage hero CTA, and reserved an exact 89px scroll margin for the request-form section so its wine surface meets the sticky header. The production form itself remains gated on the confirmed receiving service, privacy handling, and fallback contact route documented in the build plan.
- September 17, 2026 (Get Started location and FAQ slices): Created the reusable `LocationMap` and `FaqAccordion` models through Slice Machine and added both to the Page model. The location renderer derives its embedded map from an editor-controlled address and uses the shared heading, rich-text, bounded-layout, and editorial-link primitives. FAQ entries are a repeatable group of question and paragraph answer fields rendered as native disclosure controls. Both sections match the selected Get Started prototype's desktop two-column geometry, section rhythm, map sizing, hairline rules, accordion spacing, and responsive single-column layout. Publication and final rendered verification are recorded with the implementation handoff.
- September 17, 2026 (Get Started consultation form): Created and pushed the reusable `ConsultationForm` slice through Slice Machine, inserted it immediately after the Get Started hero, and populated its prototype copy and privacy link in Prismic. Added reusable input, textarea, choice-pill, loading-dot, and native action-button components; shared Zod validation; and a validated `/api/consultation` boundary. Installed React Hook Form, Zod, Resend, and React Email dependencies. Delivery deliberately returns a clear unavailable response until Resend credentials, the HTML email template, privacy policy, receiving policy, and fallback route are completed, so the interface cannot report a false send.
- September 17, 2026 (Unified wine color): Replaced the site's shared wine color with `#6A2C47`. The `wine-band` background token now aliases the single `wine` token, and the consultation form summary no longer carries a hard-coded legacy wine value, so wine bands, form surfaces, buttons, links, and related accents inherit the same global color rule.
- September 17, 2026 (Consultation form validation states): Reworked the validation presentation into an aligned red alert panel beneath “About you,” moved field-level errors farther from their controls, added red invalid-control rules, and exposed error relationships to assistive technology. Fixed the conditional contact validation so Email requires only email and Phone requires only phone even when other required fields are also incomplete. Added a development-only `consultation-preview=success` submission path and a complete success panel so the confirmation state can be reviewed without pretending an email was delivered.
- September 17, 2026 (Consultation form state refinement): Moved the validation alert above the “About you” heading and added a shared-button “Start a new request” action to the success panel. The action clears all entered values, validation errors, selections, and consent before restoring the fresh form.
- September 17, 2026 (Editable consultation success copy): Added `success_eyebrow` and `reset_label` fields to the ConsultationForm model through Slice Machine, complementing the existing editor-controlled success heading and message. Pushed the model, populated “Request received” and “Start a new request” on the Get Started document, and published the page. Verified the published CMS copy in the rendered development site, confirmed the reset action restores an empty form, and confirmed the validation alert precedes the “About you” heading.
